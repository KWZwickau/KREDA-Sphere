(function( $ )
{
    'use strict';
    $.fn.ModMathJax = function( options )
    {

        // This is the easiest way to have default options.
        var settings = $.extend( {
            // These are the defaults.
        }, options );

        return this;
    };

}( jQuery ));
