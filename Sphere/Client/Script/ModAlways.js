(function( $ )
{

    $.fn.ModAlways = function( options )
    {

        // This is the easiest way to have default options.
        var settings = $.extend( {
            // These are the defaults.
        }, options );

        // script goes here
        return this;

    };

}( jQuery ));
